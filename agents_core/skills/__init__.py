"""
agents_core.skills
====================

Демо-скиллы (доменные функции) для профилей-пайплайнов персонализации —
см. итоговый документ, раздел "Демо-скиллы для профиля «Покупки»".

Каждый модуль этого пакета — НЕЗАВИСИМАЯ от БД и от HTTP чистая логика
конкретного домена (например, `shopping_demo.py`): принимает и возвращает
обычные Python-структуры, ничего не знает о `Repository`/`Database`. Связь
между схемой функции (что видит модель в `tools`), её именем и этой чистой
логикой устанавливает `agents_core.repository.Repository` — единый реестр
"имя функции -> обработчик", общий и для встроенных функций памяти, и для
скиллов профиля (см. `Repository._TOOL_HANDLERS`).
"""
